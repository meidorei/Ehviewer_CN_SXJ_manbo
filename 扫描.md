# 追更与书签更新扫描说明喵

本文记录本地追更和书签更新扫描的当前行为、共享页数上限、降级规则以及扫描结果在右侧抽屉中的展示与清理规则喵。

## 1. 全局扫描页数设置喵

- 设置入口为“设置 → 高级 → 全局扫描页数上限”喵。
- 追更和书签的全局中文扫描共用同一个设置，允许输入 1–300 的整数，默认值为 30 页喵。
- 未保存过该设置或持久化值非法时使用默认值 30 页，不需要数据库迁移喵。
- 每次更新任务进入全局扫描前只读取一次页数上限，因此任务运行中修改设置只影响下一次任务喵。
- 扫描方式选择弹窗和前台通知使用同一个当前值，分别显示动态说明和正确的进度总页数喵。

## 2. 全局中文扫描喵

追更和书签都会从无关键词的中文全局列表第一页开始扫描，并使用“账号、站点、任务类型、查询签名”范围内的共享 `LOCAL_GLOBAL_CURSOR` 喵。

- 扫描越过上一轮共享游标后才证明覆盖完成，同秒 Gallery 的 GID 必须完整处理，不能只按时间戳提前停止喵。
- 网络错误、解析结果不完整、暂停或停止不会把未完成的全局结果错误提交，也不会错误推进共享游标喵。
- 首次没有共享游标时只用第一页建立零基线，不把已有内容计算为新增喵。
- 由旧版本升级且尚无共享游标时，会用现有单项 checkpoint 中最旧的边界作为一次性迁移起点喵。
- 扫描过程中发生 E／EX host 切换时，会清空本轮内存结果并从新 host 的第一页重新开始，但继续使用任务启动时固定的页数上限喵。

## 3. 达到页数上限后的降级喵

- 追更全局扫描达到设置的页数上限仍未越过共享游标时，会降级为逐标签检查第一页喵。
- 可精确本地匹配的书签达到页数上限仍未越过共享游标时，会降级为逐书签检查第一页喵。
- 复杂关键词、旧边界早于共享游标的桥接书签和无法从 `GalleryInfo` 精确重现条件的书签，也会进入逐书签队列喵。
- 降级队列继续沿用既有 checkpoint、`20+` 下界和 GID 去重语义，不会因为页数设置改变而重复累计新增内容喵。
- 降级队列全部成功且本轮取得了非空 `globalTop` 后，共享全局游标推进到该顶部边界，下一轮不再反复追逐同一段旧边界喵。
- 若所有书签都在全局扫描前进入桥接、没有取得全局顶部，则按普通逐书签规则处理，不伪造共享游标喵。

## 4. 状态、未读与打开边界喵

- 每个追更标签或书签都有独立的更新状态和同步 checkpoint，新增 Gallery 先按来源与 GID 写入 `LOCAL_UNREAD_GALLERY` 去重，再汇总显示数量喵。
- 从未成功建立基线时不显示数量，已建立基线但没有新增时显示 `0`，有新增时显示 `1–20`，达到下界时显示 `20+` 喵。
- 更新检查只写 `LOCAL_FOLLOW_SYNC` 或 `BOOKMARK_SYNC`，不会移动用户打开列表所使用的 `LOCAL_FOLLOW_OPEN` 或 `BOOKMARK_OPEN` 分割线喵。
- 打开追更或书签列表只清除进入页面前已经存在的未读 GID，页面加载期间新到达的内容留给下一次打开处理喵。

## 5. 验证重点喵

- 高级设置应显示当前页数和 1–300 页范围，并拒绝空值、小数、非数字、0 与 301 以上的值喵。
- 未配置设备应继续显示并使用 30 页，合法设置在重进高级设置后应保持不变喵。
- 追更和书签的普通与“推荐”全局扫描说明都应显示当前页数喵。
- 全局扫描通知进度总量应等于任务启动时读取的页数，运行中修改设置不得改变本轮总量喵。
- 达到自定义上限而未覆盖游标时，应分别进入原有逐标签或逐书签降级队列喵。
- 新增或删除资源 ID 后应执行干净 Debug 构建，避免增量编译字节码与最终资源表错位喵。
