# 追更与书签更新扫描说明

本文记录本地追更和书签更新扫描的当前行为，以及扫描结果在右侧抽屉中的展示和清理规则。

## 1. 更新状态

每个追更标签或书签都有独立的更新状态，主要包含检查时间、精确或下界计数状态以及未读 Gallery GID。

- 从未成功建立基线时，抽屉不显示数量和状态点。
- 已建立基线但没有新增内容时显示 `0` 和普通分隔点。
- 有新增内容时显示 `1–20` 和绿色圆点。
- 达到显示上限或只能确定下界时显示 `20+` 和绿色圆点。
- 语言条件冲突或不支持的书签模式显示完整诊断文字，不作为新增内容排序。

## 2. 抽屉布局与排序

追更栏和书签栏共用固定的“数量、状态点、名称”三列布局。

- 列表左侧保留 6dp 呼吸空间，正常数量列固定为 34dp 并居中显示，完整容纳 `1`、`10` 和 `20+`；非数字诊断文字会按内容扩展。
- 状态点列固定为 20dp，名称区域另有 4dp 起始内边距，避免数量、状态点和名称相贴。
- 即使未检查或数量为 `0`，也保留两列空间，因此所有正常标签名称从同一位置开始。
- 绿色圆点不是唯一的状态表达，数量文本会同时保留。
- 书签名称独占第一行并使用全部剩余宽度，保存的原始查询以 12sp 灰色文字显示在第二行，空间不足时优先省略原文；书签行最小高度为 56dp，长按书签时操作框标题下方也会显示原始查询。
- 追更名称第一行通过标签库动态显示“中文分类:标签译名”，分类随实际 namespace 变化；标签库不可用或没有分类翻译时，使用随应用语言变化的 12 种合法 namespace 资源兜底。第二行以 12sp 灰色文字显示完整原始查询。原始查询仍是打开页面、排序兜底和扫描状态关联键，展示翻译不会改变扫描语义。
- 列表仍按更新数量从高到低排序，同数量时继续使用原有的时间或原始顺序规则。

## 3. 扫描方式

追更和书签都支持全局中文扫描和逐项检查。

- 全局中文扫描最多读取 30 页，并使用“账号、站点、任务类型、查询签名”范围内的共享全局游标。
- 扫描越过上一轮游标后才证明覆盖完成；网络或解析结果不完整时不会错误推进游标。
- 追更标签使用本地标签匹配共享结果。
- 能精确本地重现的书签参加共享扫描，复杂查询或旧边界未覆盖的书签会降级为逐书签检查。
- 新增数量先按 GID 写入未读表并去重，再汇总到更新状态。
- 打开追更或书签列表只清除进入前已经存在的未读 GID，检查更新不会移动打开分割线。

## 4. 书签搜索与删除

书签齿轮管理页的搜索同时匹配显示名称和保存的查询内容，忽略大小写并保持原始顺序。

搜索期间拖拽排序被禁用，防止在过滤列表中产生不明确的持久化顺序；清空或收起搜索后恢复拖拽。

书签可以从抽屉长按菜单或齿轮管理页删除，两处都必须二次确认并共用相同清理入口。确认删除后会清理：

- `QUICK_SEARCH` 主记录。
- `LOCAL_UPDATE_STATE` 中的书签状态。
- `LOCAL_UNREAD_GALLERY` 中的书签未读 GID。
- `BOOKMARK_SYNC`、`BOOKMARK_OPEN` 和兼容 `QUICK_SEARCH` checkpoint。
- 对应的自动基线队列记录。

取消确认不会修改书签或扫描状态。

## 5. 验证重点

- 新增或删除资源 ID 后应至少执行一次干净全量构建，避免最终常量资源 ID 与增量编译字节码不同步。
- 未检查、`0`、`1`、`10`、`20`、`20+` 的名称起点一致。
- `1`、`10`、`20`、`20+` 显示绿色圆点，`0` 不显示绿色圆点。
- 搜索名称和查询内容都能命中，清空搜索后完整顺序不变。
- 搜索期间不能拖拽，退出搜索后可以继续拖拽。
- 两个删除入口都有确认框，取消无副作用，确认后不会残留更新数量或 checkpoint。
