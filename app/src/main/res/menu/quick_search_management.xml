<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_search"
        android:title="@string/search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always|collapseActionView" />

    <item
        android:id="@+id/action_bookmark_diagnostics"
        android:title="@string/bookmark_diagnostics_action_short"
        app:contentDescription="@string/bookmark_diagnostics_action"
        app:showAsAction="always|withText"
        app:tooltipText="@string/bookmark_diagnostics_action" />

</menu>
